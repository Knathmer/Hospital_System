import React from 'react';

const Settings = () => {
    return (
        <div>
            <h1>Admin Settings</h1>
            <p>This is a static test page for the admin settings section.</p>
        </div>
    );
};

export default Settings;