const button = document.querySelector('.my_button'); //grabs the specific button and stores into variable

button.addEventListener('click', () => { // anonymoose function
    console.log('Button was clicked!');
});
